# Tier-2 LLM-shell-out exemption list (ADR-030).
#
# Adding a path here requires amending ADR-030. Every entry MUST cite
# the ADR and a removal deadline immediately above the path.
#
# Format: paths are relative to the repo root, one per line.
# Lines beginning with '#' and blank lines are ignored.

# Phase A subagent dispatch — exempt per ADR-030 §"Roll-out" until the
# tier-1-orchestrated rewrite ships. The hot path of the two-phase compile
# retains its claude-CLI dependency until then; the prior v0.7.0 deadline
# has passed without the rewrite landing — removal deadline reset to
# v0.8.0. The re-orchestration decision (pure tier-1 Task dispatch,
# ADR-044 pattern, vs. a host-dispatch abstraction that still shells out)
# is tracked in BRAIN-009 Q8 — read that before starting the rewrite.
tools/edikt/internal/phasea/runner.go
