---
paths: "**/*"
compile_schema_version: 3
---
<!-- edikt:compiled — generated by gov-compile, do not edit manually -->
<!-- source: {{ .ADRCompiled }} ADRs, {{ .INVCompiled }} invariants, {{ .GuidelineCompiled }} guidelines compiled; {{ excludedNote .Excluded }} -->
<!-- compiled_by: gov-compile v{{ .CompilerVersion }} -->
<!-- directives: {{ .DirectiveCount }} across {{ .TopicCount }} topic files -->

# Governance

This file is loaded on every edit. It carries only what applies to every edit.

## Non-Negotiable Constraints

These hold everywhere, at all times. Violation is never acceptable.

{{ range .InvariantRules }}- {{ .Text }}
{{ end }}
## Topics

Everything else is scoped. Each line says when a task needs that topic; read the
named file when it matches what you are about to do.

{{ range .TopicIndex }}- **{{ .Topic }}** — {{ if .Description }}{{ .Description }}{{ else }}_(no approved description — pending)_{{ end }}
{{ end }}
Scoped rules load automatically when you touch a file they cover. Reminders and
verification items are delivered at write time, not from this file.
