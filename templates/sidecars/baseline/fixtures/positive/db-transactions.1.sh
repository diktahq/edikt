#!/usr/bin/env bash
# Positive fixture for db-transactions directive 1
# Property: Transaction handles are passed explicitly rather than via thread-local storage
# In an adopter project this would actually exercise the behavior; here it is
# an illustrative stub that always passes (exit 0) to represent compliant code.
exit 0
