#!/usr/bin/env bash
# Positive fixture for owasp-baseline directive 0
# Property: Responses set CSP, X-Content-Type-Options, and HSTS headers via shared middleware
# In an adopter project this would actually exercise the behavior; here it is
# an illustrative stub that always passes (exit 0) to represent compliant code.
exit 0
