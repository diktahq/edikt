#!/usr/bin/env bash
# Positive fixture for frontend-page directive 1
# Property: Pages fetching user-specific data redirect unauthenticated visitors to sign-in
# In an adopter project this would actually exercise the behavior; here it is
# an illustrative stub that always passes (exit 0) to represent compliant code.
exit 0
