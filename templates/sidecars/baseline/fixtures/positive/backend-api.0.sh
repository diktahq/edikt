#!/usr/bin/env bash
# Positive fixture for backend-api directive 0
# Property: API handlers return structured error responses with a top-level error key on non-2xx responses
# In an adopter project this would actually exercise the behavior; here it is
# an illustrative stub that always passes (exit 0) to represent compliant code.
exit 0
