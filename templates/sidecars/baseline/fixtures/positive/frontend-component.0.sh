#!/usr/bin/env bash
# Positive fixture for frontend-component directive 0
# Property: Components declare a typed Props interface with required vs optional clearly distinguished
# In an adopter project this would actually exercise the behavior; here it is
# an illustrative stub that always passes (exit 0) to represent compliant code.
exit 0
