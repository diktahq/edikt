#!/usr/bin/env bash
# Positive fixture for db-queries directive 0
# Property: SQL queries use parameterized placeholders rather than string concatenation
# In an adopter project this would actually exercise the behavior; here it is
# an illustrative stub that always passes (exit 0) to represent compliant code.
exit 0
