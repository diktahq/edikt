#!/usr/bin/env bash
# Positive fixture for backend-api directive 1
# Property: Protected endpoints reject requests missing the Authorization header with 401
# In an adopter project this would actually exercise the behavior; here it is
# an illustrative stub that always passes (exit 0) to represent compliant code.
exit 0
