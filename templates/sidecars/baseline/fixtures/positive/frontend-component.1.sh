#!/usr/bin/env bash
# Positive fixture for frontend-component directive 1
# Property: Interactive components expose an accessible name (aria-label, aria-labelledby, or text content)
# In an adopter project this would actually exercise the behavior; here it is
# an illustrative stub that always passes (exit 0) to represent compliant code.
exit 0
