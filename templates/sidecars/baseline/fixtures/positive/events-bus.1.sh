#!/usr/bin/env bash
# Positive fixture for events-bus directive 1
# Property: Event handlers are idempotent — replaying an event produces no duplicate side effects
# In an adopter project this would actually exercise the behavior; here it is
# an illustrative stub that always passes (exit 0) to represent compliant code.
exit 0
