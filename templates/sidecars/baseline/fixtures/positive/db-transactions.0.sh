#!/usr/bin/env bash
# Positive fixture for db-transactions directive 0
# Property: Multi-table writes are wrapped in a transaction that rolls back on error
# In an adopter project this would actually exercise the behavior; here it is
# an illustrative stub that always passes (exit 0) to represent compliant code.
exit 0
