#!/usr/bin/env bash
# Negative fixture for backend-api directive 1
# Property: Protected endpoints reject requests missing the Authorization header with 401
# In an adopter project this would exercise the violating shape; here it is
# an illustrative stub that always fails (exit 1) to represent violating code.
exit 1
