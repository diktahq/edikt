#!/usr/bin/env bash
# Negative fixture for frontend-component directive 1
# Property: Interactive components expose an accessible name (aria-label, aria-labelledby, or text content)
# In an adopter project this would exercise the violating shape; here it is
# an illustrative stub that always fails (exit 1) to represent violating code.
exit 1
