#!/usr/bin/env bash
# Negative fixture for db-queries directive 0
# Property: SQL queries use parameterized placeholders rather than string concatenation
# In an adopter project this would exercise the violating shape; here it is
# an illustrative stub that always fails (exit 1) to represent violating code.
exit 1
