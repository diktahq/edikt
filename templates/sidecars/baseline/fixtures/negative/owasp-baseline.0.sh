#!/usr/bin/env bash
# Negative fixture for owasp-baseline directive 0
# Property: Responses set CSP, X-Content-Type-Options, and HSTS headers via shared middleware
# In an adopter project this would exercise the violating shape; here it is
# an illustrative stub that always fails (exit 1) to represent violating code.
exit 1
