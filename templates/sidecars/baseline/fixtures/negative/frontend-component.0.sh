#!/usr/bin/env bash
# Negative fixture for frontend-component directive 0
# Property: Components declare a typed Props interface with required vs optional clearly distinguished
# In an adopter project this would exercise the violating shape; here it is
# an illustrative stub that always fails (exit 1) to represent violating code.
exit 1
