#!/usr/bin/env bash
# Negative fixture for backend-api directive 0
# Property: API handlers return structured error responses with a top-level error key on non-2xx responses
# In an adopter project this would exercise the violating shape; here it is
# an illustrative stub that always fails (exit 1) to represent violating code.
exit 1
