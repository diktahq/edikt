---
title: Override Config with Frontmatter
status: accepted
database_type: document-mongo
---

# Override Config with Frontmatter

This spec explicitly specifies MongoDB in frontmatter, overriding config.

## Requirements

Store documents in MongoDB despite config saying SQL.

## Data Model

The system uses MongoDB for document storage.
