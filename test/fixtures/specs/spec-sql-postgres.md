---
title: SQL Feature - Postgres
status: accepted
---

# SQL Feature with Postgres

This feature uses Postgres as the relational database.

## Requirements

Store user data in Postgres with proper indexing.

## Data Model

The system stores users, posts, and comments in Postgres with foreign key relationships.
