=== prompt ===
Add a new HTTP handler for `GET /orders` that returns the user's orders as JSON. The user ID is in the request context as `ctx.Value("userID").(string)`.

=== claude exit: 0 ===

=== claude output ===
Builds clean. Here's what was added:

**`internal/http/handlers/orders.go`** — `GetOrders` handler that:
- Guards on `tenantID` from context (401 if missing) — matching the existing tenant-isolation pattern used by `GetUser` and `ListProducts`
- Guards on `userID` from context (401 if missing)
- Returns orders as JSON, scoped to both user and tenant

**`main.go`** — registered `GET /orders → handlers.GetOrders`

In a real implementation you'd replace the canned slice with `orderRepo.FindOrdersByUserAndTenant(ctx, userID, tenantID)` — the repository already enforces both filters at the SQL level (`repository.go:65-68`).

=== files modified ===
./go.mod
./internal/middleware/tenant.go
./internal/http/handlers/users.go
./internal/http/handlers/orders.go
./internal/http/handlers/products.go
./internal/orders/repository.go
./main.go
