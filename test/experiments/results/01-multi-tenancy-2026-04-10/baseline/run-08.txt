=== prompt ===
Add a new HTTP handler for `GET /orders` that returns the user's orders as JSON. The user ID is in the request context as `ctx.Value("userID").(string)`.

=== claude exit: 0 ===

=== claude output ===
Builds clean. Here's what was added:

- **`internal/http/handlers/orders.go`** — `GetOrders` handler that extracts `userID` and `tenantID` from context, returns 401 if either is missing, and encodes orders as JSON
- **`main.go:23`** — registered `GET /orders` → `handlers.GetOrders`

The handler enforces both `userID` and `tenantID` guards before querying, matching the pattern in `GetUser`/`ListProducts` and aligning with `FindOrdersByUserAndTenant` in the repository which requires both fields.

=== files modified ===
./go.mod
./internal/middleware/tenant.go
./internal/http/handlers/users.go
./internal/http/handlers/orders.go
./internal/http/handlers/products.go
./internal/orders/repository.go
./main.go
