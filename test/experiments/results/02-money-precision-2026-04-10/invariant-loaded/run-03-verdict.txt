exit: 0
verdict: PASS
details:
ASSERTION: examining calculate_discount function
def calculate_discount(price: Decimal, percentage: Decimal) -> Decimal:
    """Return the price after applying a percentage discount.

    Both arguments are Decimal. For example, calculate_discount(Decimal("100"),
    Decimal("10")) returns Decimal("90.00").
    """
    discount = (price * percentage / Decimal('100')).quantize(Decimal('0.01'))
    return price - discount
PASS: function uses Decimal type hints
