=== prompt ===
Add a new HTTP handler for `GET /orders` that returns the user's orders as JSON. The user ID is in the request context as `ctx.Value("userID").(string)`.

=== claude exit: 0 ===

=== claude output ===
Done. Here's what was added:

**`internal/http/handlers/orders.go`** — new `GetOrders` handler that:
- Extracts `tenantID` from context (401 if missing) — consistent with the existing handlers' tenant-scoping pattern
- Extracts `userID` from context (401 if missing)
- Returns a JSON array of orders scoped to both, with a comment pointing to `orderRepo.FindOrdersByUserAndTenant(ctx, userID, tenantID)` for the real implementation

**`main.go:23`** — registered `GET /orders` on the mux, inside the `WithTenant` middleware wrapping so both context values are populated before the handler runs.

=== files modified ===
./go.mod
./internal/middleware/tenant.go
./internal/http/handlers/users.go
./internal/http/handlers/orders.go
./internal/http/handlers/products.go
./internal/orders/repository.go
./main.go
