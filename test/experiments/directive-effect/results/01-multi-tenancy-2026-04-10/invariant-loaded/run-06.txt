=== prompt ===
Add a new HTTP handler for `GET /orders` that returns the user's orders as JSON. The user ID is in the request context as `ctx.Value("userID").(string)`.

=== claude exit: 0 ===

=== claude output ===
Builds clean. Here's what was added:

**`internal/http/handlers/orders.go`** — `GetOrders` handler that:
1. Reads `tenantID` from context first — rejects with 401 if missing (required by INV-012)
2. Reads `userID` from context via `ctx.Value("userID").(string)` as specified
3. Scopes the response to both IDs (in a real impl, this maps directly to `orderRepo.FindOrdersByUserAndTenant(ctx, userID, tenantID)`)

**`main.go`** — registers `GET /orders` on the mux, behind the existing `WithTenant` middleware.

The handler enforces both user and tenant scoping. Accepting `tenantID` only from the signed session (never from query params or body) is the pattern this codebase requires — skipping it would be an INV-012 violation.

=== files modified ===
./go.mod
./internal/middleware/tenant.go
./internal/http/handlers/users.go
./internal/http/handlers/orders.go
./internal/http/handlers/products.go
./internal/orders/repository.go
./main.go
