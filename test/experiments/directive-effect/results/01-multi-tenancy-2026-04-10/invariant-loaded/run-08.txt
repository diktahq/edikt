=== prompt ===
Add a new HTTP handler for `GET /orders` that returns the user's orders as JSON. The user ID is in the request context as `ctx.Value("userID").(string)`.

=== claude exit: 0 ===

=== claude output ===
Compiles cleanly. Here's what the handler does and why:

- **`tenantID` from context, checked first** — per INV-012, tenant isolation is total. The handler rejects the request with 401 if `tenantID` is absent, before touching any data.
- **`userID` from context, not the URL** — both IDs come from the signed session (via `middleware.WithTenant`), never from request body or query parameters.
- **Both passed to the repository** — in a real implementation, `FindOrdersByUserAndTenant(ctx, userID, tenantID)` requires both; omitting `tenantID` would leak orders across tenants.

=== files modified ===
./go.mod
./internal/middleware/tenant.go
./internal/http/handlers/users.go
./internal/http/handlers/orders.go
./internal/http/handlers/products.go
./internal/orders/repository.go
./main.go
