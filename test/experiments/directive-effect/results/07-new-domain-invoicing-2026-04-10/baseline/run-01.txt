=== prompt ===
Add an invoicing feature. After checkout, an invoice should be generated with line items, subtotal, tax (calculate at 20%), and total. Invoices need their own database table and queries. Users should be able to list their invoices and get a specific invoice by ID. Add the full stack: domain types, repository with SQL, service layer, HTTP handlers, and routes.

=== claude exit: 0 ===

=== claude output ===
Clean build — no output from `go build` means it compiled without errors.

Here's what was added:

**New files:**

- `internal/domain/invoice.go` — `Invoice` and `InvoiceLineItem` types
- `internal/repository/invoices.go` — `InvoicesRepo` with three queries:
  - `Create` — inserts invoice + line items in a single transaction
  - `GetByID` — fetches invoice header + line items, tenant-scoped
  - `ListByUser` — fetches invoice headers for a user, ordered newest-first
- `internal/service/invoices.go` — `InvoiceService` for list/get, pulls `UserID` from context
- `internal/handler/invoices.go` — `InvoicesHandler` with `List` and `GetByID` (uses `r.PathValue` for the Go 1.22 path parameter)
- `db/invoices.sql` — schema for the two new tables

**Modified files:**

- `internal/service/checkout.go` — accepts `*InvoicesRepo`, builds line items and tax from cart, creates the invoice after the order, returns `(*domain.Invoice, error)` instead of `error`
- `internal/handler/checkout.go` — handles the new return value, includes `invoice_id` in the success response
- `cmd/main.go` — wires the new repo/service/handler and registers two new routes

**New routes:**
```
GET /invoices        → list authenticated user's invoices
GET /invoices/{id}   → get a specific invoice with line items
```

**Tax calculation:** `tax = subtotal / 5` (integer division, truncates to whole cents — same rounding behaviour as the rest of the codebase).

=== files modified ===
./cmd/main.go
./go.mod
./internal/handler/checkout.go
./internal/handler/invoices.go
./internal/middleware/auth.go
./internal/repository/carts.go
./internal/repository/orders.go
./internal/repository/invoices.go
./internal/service/checkout.go
./internal/service/invoices.go
./internal/domain/cart.go
./internal/domain/order.go
./internal/domain/invoice.go
./db/invoices.sql
