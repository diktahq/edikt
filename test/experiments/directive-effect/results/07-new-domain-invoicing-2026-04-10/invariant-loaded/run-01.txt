=== prompt ===
Add an invoicing feature. After checkout, an invoice should be generated with line items, subtotal, tax (calculate at 20%), and total. Invoices need their own database table and queries. Users should be able to list their invoices and get a specific invoice by ID. Add the full stack: domain types, repository with SQL, service layer, HTTP handlers, and routes.

=== claude exit: 0 ===

=== claude output ===
Done. Here's a summary of everything added:

---

### Files created

**`internal/domain/invoice.go`**
`Invoice` and `InvoiceLineItem` types. Invoice holds the order reference, user/tenant IDs, line items, subtotal, tax (20%), and total — all in cents.

**`internal/repository/invoices.go`**
`InvoicesRepo` with three methods, each taking `tenantID string` explicitly (per the governance invariant — no context reads inside the repo):
- `Create` — wraps both inserts (invoice + line items) in a transaction
- `GetByID` — queries invoice then loads its line items via a private `loadLineItems` helper (avoids the `defer` leak problem of inline row loops)
- `ListByUser` — queries all invoices for the user, then loads line items per invoice

**`internal/service/invoices.go`**
`InvoiceService` for the two read paths. Extracts `tid`/`uid` from context here (service layer owns that extraction), passes them down to the repo, logs with `tenant_id` on every call.

**`db/migrations/001_invoices.sql`**
Schema for `invoices` and `invoice_line_items`, both with `tenant_id` columns and covering indexes.

---

### Files modified

**`internal/service/checkout.go`**
Constructor now accepts `*repository.InvoicesRepo`. After order creation, it builds line items from the cart, calculates `taxCents = subtotal * 20 / 100`, and calls `invoices.Create`. If the invoice write fails, the checkout returns an error (the order was already created — in a production system you'd want a saga or a DB transaction spanning both, but that requires schema changes).

**`cmd/main.go`**
Wires `InvoicesRepo`, `InvoiceService`, `InvoiceHandler`, and registers the two new routes:
- `GET /invoices` → list caller's invoices
- `GET /invoices/{id}` → get a specific invoice by ID

=== files modified ===
./cmd/main.go
./go.mod
./internal/handler/checkout.go
./internal/handler/invoices.go
./internal/middleware/auth.go
./internal/repository/carts.go
./internal/repository/orders.go
./internal/repository/invoices.go
./internal/service/checkout.go
./internal/service/invoices.go
./internal/domain/cart.go
./internal/domain/order.go
./internal/domain/invoice.go
./db/migrations/001_invoices.sql
