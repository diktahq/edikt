schema_version: 1
topic: extensibility
path: docs/architecture/decisions/ADR-005-extensibility-model.md
signals: []
directives:
  - text: 'Project templates in `.edikt/templates/` take precedence over edikt defaults. Lookup order: project override → edikt default. Report overrides in `/edikt:doctor`. (ref: ADR-005)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'Project templates in `.edikt/templates/` take precedence over edikt defaults. Lookup order: project override → edikt default. Report overrides in `/edikt:doctor`. (ref: ADR-005)'
  - text: 'Agent customization: mark with `<!-- edikt:custom -->` or list in `agents.custom` in config. Upgrade MUST skip customized agents without prompting. (ref: ADR-005)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'Agent customization: mark with `<!-- edikt:custom -->` or list in `agents.custom` in config. Upgrade MUST skip customized agents without prompting. (ref: ADR-005)'
  - text: 'Rule packs: override by placing a file at `.edikt/rules/{name}.md` without the `edikt:generated` marker, or extend via config. Upgrade MUST skip files without the marker. (ref: ADR-005)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'Rule packs: override by placing a file at `.edikt/rules/{name}.md` without the `edikt:generated` marker, or extend via config. Upgrade MUST skip files without the marker. (ref: ADR-005)'
paths:
  - commands/upgrade.md
  - commands/doctor.md
  - commands/rules-update.md
  - commands/init.md
scope:
  - implementation
