schema_version: 1
topic: hooks
path: docs/architecture/decisions/ADR-019-security-override-of-byte-for-byte-hook-preservation.md
signals: []
directives:
  - text: 'Security-driven rewrites MAY change user-visible hook content AND JSON wrapping in the same commit, but ONLY for: `templates/hooks/subagent-stop.sh`, `templates/hooks/file-changed.sh`, `templates/hooks/headless-ask.sh`, `templates/hooks/stop-failure.sh`. (ref: ADR-019)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'Security-driven rewrites MAY change user-visible hook content AND JSON wrapping in the same commit, but ONLY for: `templates/hooks/subagent-stop.sh`, `templates/hooks/file-changed.sh`, `templates/hook'
  - text: 'The carve-out applies only when the existing hook content violates INV-003 or INV-004. Style rewrites, UX polish, or unrelated changes MUST follow ADR-014 unaltered. (ref: ADR-019)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'The carve-out applies only when the existing hook content violates INV-003 or INV-004. Style rewrites, UX polish, or unrelated changes MUST follow ADR-014 unaltered. (ref: ADR-019)'
  - text: 'Fresh characterization fixtures MUST be regenerated by running the updated hook against its payload, per ADR-011. Hand-authored expected outputs remain forbidden. (ref: ADR-019)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'Fresh characterization fixtures MUST be regenerated by running the updated hook against its payload, per ADR-011. Hand-authored expected outputs remain forbidden. (ref: ADR-019)'
  - text: 'Any future hook content change outside the four listed hooks MUST follow ADR-014''s byte-for-byte rule. This carve-out does NOT generalize. (ref: ADR-019)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'Any future hook content change outside the four listed hooks MUST follow ADR-014''s byte-for-byte rule. This carve-out does NOT generalize. (ref: ADR-019)'
paths:
  - templates/hooks/subagent-stop.sh
  - templates/hooks/file-changed.sh
  - templates/hooks/headless-ask.sh
  - templates/hooks/stop-failure.sh
scope:
  - implementation
  - review
