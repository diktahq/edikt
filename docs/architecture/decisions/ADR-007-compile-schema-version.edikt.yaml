schema_version: 1
topic: compile
path: docs/architecture/decisions/ADR-007-compile-schema-version.md
signals: []
directives:
  - text: 'Generated governance.md MUST have `compile_schema_version` in YAML frontmatter, set to the integer constant declared in `commands/gov/compile.md`. NEVER stamp edikt marketing version into this field. (ref: ADR-007)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'Generated governance.md MUST have `compile_schema_version` in YAML frontmatter, set to the integer constant declared in `commands/gov/compile.md`. NEVER stamp edikt marketing version into this field. '
  - text: '`/edikt:doctor` MUST treat missing `compile_schema_version` as schema version 1 (legacy). If the file''s schema version is less than the current constant, recommend `/edikt:gov:compile` to regenerate. (ref: ADR-007)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: '`/edikt:doctor` MUST treat missing `compile_schema_version` as schema version 1 (legacy). If the file''s schema version is less than the current constant, recommend `/edikt:gov:compile` to regenerate. '
  - text: 'Bump `COMPILE_SCHEMA_VERSION` in `commands/gov/compile.md` ONLY when the output structure changes in a way that older tooling cannot read. Bug fixes, prose changes, and directive updates do NOT bump the schema. (ref: ADR-007)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: Bump `COMPILE_SCHEMA_VERSION` in `commands/gov/compile.md` ONLY when the output structure changes in a way that older tooling cannot read. Bug fixes, prose changes, and directive updates do NOT bump t
  - text: '`compiled_by` and `compiled_at` MUST remain in HTML comments (not YAML frontmatter). They are human diagnostics, not programmatic contracts. Never enforce equality on them in tests or in doctor/upgrade logic. (ref: ADR-007)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: '`compiled_by` and `compiled_at` MUST remain in HTML comments (not YAML frontmatter). They are human diagnostics, not programmatic contracts. Never enforce equality on them in tests or in doctor/upgrad'
paths:
  - commands/gov/compile.md
  - commands/doctor.md
  - commands/upgrade.md
scope:
  - implementation
