schema_version: 1
topic: agent-rules
path: docs/architecture/decisions/ADR-025-evaluator-verdict-storage-config-driven-path.md
signals: []
directives:
  - text: 'Evaluator agents MUST emit a single JSON object conforming to `templates/agents/evaluator-verdict.schema.json`. NEVER emit prose verdicts. (ref: ADR-025)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'Evaluator agents MUST emit a single JSON object conforming to `templates/agents/evaluator-verdict.schema.json`. NEVER emit prose verdicts. (ref: ADR-025)'
  - text: 'The plan harness MUST reject PASS unless every criterion whose id references a shell command has `evidence_type: "test_run"`. Mismatch forces the verdict to BLOCKED with a listed reason. (ref: ADR-025)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'The plan harness MUST reject PASS unless every criterion whose id references a shell command has `evidence_type: "test_run"`. Mismatch forces the verdict to BLOCKED with a listed reason. (ref: ADR-025'
  - text: 'Verdicts are persisted at `<paths.plans>/verdicts/<plan>/<phase>.json`, where `<paths.plans>` is resolved from `.edikt/config.yaml`. NEVER hardcode a literal verdicts path. (ref: ADR-025)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'Verdicts are persisted at `<paths.plans>/verdicts/<plan>/<phase>.json`, where `<paths.plans>` is resolved from `.edikt/config.yaml`. NEVER hardcode a literal verdicts path. (ref: ADR-025)'
  - text: 'Upgrade from < 0.5.0 MUST grandfather existing `done` phases by writing verdict stubs with `meta.grandfathered: true` under the resolved `<paths.plans>/verdicts/` path. Grandfathered verdicts bypass the evidence gate on a one-time basis. (ref: ADR-025)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'Upgrade from < 0.5.0 MUST grandfather existing `done` phases by writing verdict stubs with `meta.grandfathered: true` under the resolved `<paths.plans>/verdicts/` path. Grandfathered verdicts bypass t'
  - text: '`bin/edikt upgrade` MUST migrate any legacy `docs/product/plans/verdicts/` content to the resolved `<paths.plans>/verdicts/` location for projects whose `paths.plans` is no longer `docs/product/plans`. The migration MUST preserve filenames and contents byte-for-byte. (ref: ADR-025)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: '`bin/edikt upgrade` MUST migrate any legacy `docs/product/plans/verdicts/` content to the resolved `<paths.plans>/verdicts/` location for projects whose `paths.plans` is no longer `docs/product/plans`'
paths:
  - templates/agents/evaluator.md
  - templates/agents/evaluator-headless.md
  - templates/agents/evaluator-verdict.schema.json
  - templates/hooks/phase-end-detector.sh
  - commands/sdlc/plan.md
  - tools/edikt/cmd/upgrade.go
scope:
  - implementation
  - review
