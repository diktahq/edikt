schema_version: 1
topic: compile
path: docs/architecture/decisions/ADR-027-sidecar-architecture-for-governance-metadata.md
signals: []
directives:
  - text: 'edikt MUST NOT write to any ADR, invariant, or guideline `.md` file. Generated directive metadata MUST live in a co-located `<name>.edikt.yaml` sidecar conforming to `templates/schemas/sidecar.schema.json`. (ref: ADR-027)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: edikt MUST NOT write to any ADR, invariant, or guideline `.md` file. Generated directive metadata MUST live in a co-located `<name>.edikt.yaml` sidecar conforming to `templates/schemas/sidecar.schema.
  - text: 'The sidecar schema MUST NOT persist `source_hash`, `agent_prompt_version`, or `directives_hash` at the root. Hashes are recomputed on read at compile time. (ref: ADR-027)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'The sidecar schema MUST NOT persist `source_hash`, `agent_prompt_version`, or `directives_hash` at the root. Hashes are recomputed on read at compile time. (ref: ADR-027)'
  - text: '`/edikt:adr:new`, `/edikt:invariant:new`, `/edikt:guideline:new` MUST generate the `(parent.md, parent.edikt.yaml)` pair atomically by dispatching extraction to a forked subagent (`context: fork`) with a locked extraction prompt. (ref: ADR-027)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: '`/edikt:adr:new`, `/edikt:invariant:new`, `/edikt:guideline:new` MUST generate the `(parent.md, parent.edikt.yaml)` pair atomically by dispatching extraction to a forked subagent (`context: fork`) wit'
  - text: 'Per-artifact `:compile` commands MUST regenerate the sidecar in a fresh subagent context per artifact. NEVER pipe multiple artifacts through one parent session. (ref: ADR-027)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: 'Per-artifact `:compile` commands MUST regenerate the sidecar in a fresh subagent context per artifact. NEVER pipe multiple artifacts through one parent session. (ref: ADR-027)'
  - text: 'v0.6.0 `gov:compile` MUST read sidecars only. Pre-migration projects MUST exit non-zero with a single-line actionable error directing the user to `edikt migrate sidecars`. NEVER fall back to in-body sentinel parsing. (ref: ADR-027)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: v0.6.0 `gov:compile` MUST read sidecars only. Pre-migration projects MUST exit non-zero with a single-line actionable error directing the user to `edikt migrate sidecars`. NEVER fall back to in-body s
  - text: 'INV-005''s managed-region guard NARROWS in v0.6.0 to `CLAUDE.md` and `settings.json` only. Governance artifacts (`.md` files in decisions/, invariants/, guidelines/) are NEVER managed regions because edikt does not write to them. (ref: ADR-027, INV-005)'
    source_excerpt:
      line_start: 1
      line_end: 1
      quote: INV-005's managed-region guard NARROWS in v0.6.0 to `CLAUDE.md` and `settings.json` only. Governance artifacts (`.md` files in decisions/, invariants/, guidelines/) are NEVER managed regions because e
paths:
  - tools/edikt/cmd/gov.go
  - tools/edikt/internal/compile/**
  - templates/schemas/sidecar.schema.json
  - commands/adr/new.md
  - commands/adr/compile.md
  - commands/adr/review.md
  - commands/invariant/new.md
  - commands/invariant/compile.md
  - commands/invariant/review.md
  - commands/guideline/new.md
  - commands/guideline/compile.md
  - commands/guideline/review.md
  - commands/gov/compile.md
  - docs/architecture/decisions/**/*.edikt.yaml
  - docs/architecture/invariants/**/*.edikt.yaml
  - docs/guidelines/**/*.edikt.yaml
scope:
  - design
  - implementation
  - review
