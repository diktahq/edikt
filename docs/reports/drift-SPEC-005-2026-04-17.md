---
type: drift-report
spec: SPEC-005
scope: full-chain (PRD + spec + artifacts + ADRs + invariants)
date: 2026-04-17
summary:
  compliant: 22
  likely_compliant: 2
  diverged: 0
  unknown: 1
---

DRIFT REPORT — SPEC-005
─────────────────────────────────────────────────
Source: SPEC-005 + 4 artifacts (data-model, fixtures, model.mmd, test-strategy) + 5 ADRs (001, 005, 007, 008, 015) + 2 invariants (001, 002)
Scope:  full-chain — PRD-003 ACs + SPEC-005 requirements + artifact contracts + ADR compliance + invariant compliance
Date:   2026-04-17

SUMMARY
  ✅ 22 compliant    🟡 2 likely compliant
  ⚠️  0 diverged      ❓ 1 unknown

┌─────┬────────┬───────────────────────────────────────────────────────────────────────────────┐
│  #  │ Status │ Description                                                                   │
├─────┼────────┼───────────────────────────────────────────────────────────────────────────────┤
│  1  │  🟡    │ SPEC-005 §Testing Strategy "end-to-end full benchmark against this repo's     │
│     │        │ governance (dogfood) at N=1": deferred. All 17 directives lack                │
│     │        │ `behavioral_signal` — correct pre-migration state; populated only after       │
│     │        │ `/edikt:adr:review --backfill`. Documented in CHANGELOG.md v0.6.0 Baseline    │
│     │        │ block and docs/reports/governance-benchmark-baseline/README.md.               │
│     │        │ Matches Phase 10 AC: deferred is explicit, not silent.                        │
├─────┼────────┼───────────────────────────────────────────────────────────────────────────────┤
│  2  │  🟡    │ AC-010 sandbox parity: test asserts byte-equal across 4 fixture shapes        │
│     │        │ (minimal/realistic/mixed/edge) ✓. Commit-date parity lint described in SPEC   │
│     │        │ §AC-010 as "soft parity signal" — Phase 9 summary lists docstring invariant   │
│     │        │ added to runner.py; explicit CI lint step not verified in this session.       │
│     │        │ Evidence: sandbox.py:8-18 has paired-edit invariant; test_benchmark_sandbox_  │
│     │        │ parity.py covers 4 shapes. Soft signal sufficient per spec wording.           │
├─────┼────────┼───────────────────────────────────────────────────────────────────────────────┤
│  3  │  ❓    │ AC-006c SIGINT mid-call cleanup: unit test stubs SDK with blocking response   │
│     │        │ and asserts ≤5s budget via asyncio.wait_for (run.py:284).                     │
│     │        │ Real-subprocess cleanup ("no child claude subprocess persists") can only be   │
│     │        │ proven under live SDK auth — not exercised in this session. Deferred to       │
│     │        │ Phase 10 dogfood run.                                                         │
└─────┴────────┴───────────────────────────────────────────────────────────────────────────────┘

─────────────────────────────────────────────────
COMPLIANT DETAIL (22 items verified against code)

Layer 1 — PRD-003 acceptance criteria (inherited by SPEC-005): 25/25 ACs (AC-001 through AC-025) have tests citing them. Verified in phase summaries.

Layer 2 — Spec requirements (7 layers):
  ✓ Layer 1 (sentinel schema): `canonical_phrases` + `behavioral_signal` parsed as optional;
    test_adr_sentinel_integrity.py extended (Phase 5). Missing = []/{} no error (AC-002a).
  ✓ Layer 2 (orphan detection + compile history): atomic rename + 5 transition states +
    .gitignore auto-append — commands/gov/compile.md §12d, test_compile_orphan_detection.py
    47 tests pass (AC-003/003b/017/018/019).
  ✓ Layer 3 (doctor source-file check): commands/doctor.md extended (Phase 2);
    test_doctor_source_check.py asserts literal missing path in stderr (AC-004).
  ✓ Layer 4 (shared directive-quality procedure): commands/gov/_shared-directive-checks.md
    with leading-underscore note; invoked by compile and review; 40 tests, byte-identical
    output across callers (AC-002b/002c/003c/021).
  ✓ Layer 5 (benchmark command + Python helper + tier-2 install): commands/gov/benchmark.md
    `tier: 2` frontmatter + tools/gov-benchmark/ + bin/edikt install/uninstall verbs;
    51 tests pass; install.sh contains zero benchmark references (AC-005/006/007/014/015/016/023).
  ✓ Layer 6 (attack catalog): 4 templates + README + discriminative-power harness;
    45 tests pass (AC-008/013/020/025).
  ✓ Layer 7 (ADR authoring): adr:new interview prompts (3 tests) + adr:review soft-language
    scan (10 tests) + --backfill flag (7 tests) — AC-011/012/022.

Layer 3 — Artifact contracts:
  ✓ data-model.schema.yaml §1 (extended sentinel schema) — parser implements it (Phase 5).
  ✓ data-model.schema.yaml §2 (compile-history.json) — written by compile, schema validated in tests.
  ✓ data-model.schema.yaml §3+§4 (summary.json + attack-log.jsonl) — schema assertions in
    test_benchmark_report_schema.py.
  ✓ fixtures.yaml scenarios — consumed by Phases 2–9 test suites.
  ✓ test-strategy.md — unit/integration/e2e split realized; 204 new tests across phases.
  — model.mmd (status: accepted, reference-only) — no implementation required.

Layer 4 — ADR compliance:
  ✓ ADR-001 (Claude Code only, markdown-only core): tier-1 files remain .md; Python isolated
    under tools/gov-benchmark/; tier-2 install is opt-in.
  ✓ ADR-005 (extensibility): attack templates support per-project override via
    templates/attacks/ (templates/attacks/README.md §Override).
  ✓ ADR-007 (compile schema v2): schema NOT bumped — new fields additive per spec §Dependencies.
  ✓ ADR-008 (three-list schema, hash-cached): source_hash + directives_hash recomputed in
    --backfill flow (test_post_backfill_hashes_validate passes).
  ✓ ADR-015 (tier-2 carve-out): directives in .claude/rules/governance/architecture.md mirror
    ADR-015's Decision section; install.sh byte-clean of benchmark refs; exact pin in
    pyproject.toml; venv isolation; SHA-256 wheel verification.

Layer 5 — Invariant compliance:
  ✓ INV-001 (plain markdown, no build step): all tier-1 command files are pure markdown;
    tier-2 Python is explicitly carved out by ADR-015.
  ✓ INV-002 (ADRs immutable once accepted): refuse_edit_matching_frontmatter template
    specifically covers this invariant (AC-025); no ADR edits detected in changeset beyond
    status promotions (spec artifacts accepted → in-progress, which is metadata, not body).

─────────────────────────────────────────────────

✅ Drift report saved: docs/reports/drift-SPEC-005-2026-04-17.md

All clear — implementation matches governance. Two likely-compliant items are documented deferrals (baseline pending --backfill; CI lint is soft). One unknown item (SIGINT subprocess cleanup) is testable only under live SDK auth.

Next: run `/edikt:adr:review --backfill` to populate behavioral_signal fields, then run `/edikt:gov:benchmark` to capture the real baseline and close finding #1.
